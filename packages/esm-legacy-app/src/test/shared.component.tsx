import React from 'react';
import ActiveVisitsTable from '@openmrs/esm-service-queues-app/src/active-visits/active-visits-table.component';

const Shared: React.FC = () => {
  return <ActiveVisitsTable />;
};

export default Shared;
